import { VerificarMonedaDirective } from './verificar-moneda.directive';

describe('VerificarMonedaDirective', () => {
  it('should create an instance', () => {
    const directive = new VerificarMonedaDirective();
    expect(directive).toBeTruthy();
  });
});
